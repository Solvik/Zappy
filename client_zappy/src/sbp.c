#include	<stdlib.h>
#include	"visu_func.h"
#include	"client_zappy.h"

int		visu_sbp(t_fds *pooler, t_visu *visu, t_cmd *cmd)
{
  (void)pooler;
  (void)visu;
  (void)cmd;
  printf("Wrong parameter for the command\n");
  return (1);
}
